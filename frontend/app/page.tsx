import ChatInterface from "../components/ChatInterface";
import Layout from "../components/Layout";

const Home: React.FC = () => {
  return (
    <Layout>
      <ChatInterface />
    </Layout>
  );
};

export default Home;
